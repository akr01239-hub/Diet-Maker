# Render (and any host) sets NODE_ENV=production, which makes `npm ci` omit
# devDependencies. Our build needs them (typescript, tsx, prisma, @types/*),
# so force dev deps to install. `include` overrides the implicit omit=dev.
include=dev
